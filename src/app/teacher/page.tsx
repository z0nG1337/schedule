import { TeacherApp } from "@/components/teacher-app";

export const metadata = {
  title: "Расписание — преподавателям",
  description: "Расписание уроков для преподавателей",
};

export default function TeacherPage() {
  return <TeacherApp />;
}
